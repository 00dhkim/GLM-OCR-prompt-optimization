# Codex Log

## 2026-03-13

### 초기 구현 판단

- 판단해야 하는 것: 저장소가 비어 있는 상태에서 어떤 기술 스택으로 PRD 전체를 구현할지.
- 고려한 선택지: Python + CLI / Node.js + CLI.
- 판단: Python + `uv` 기반으로 구현.
- 이유: 로컬 OCR 실험, JSONL/CSV 기반 실험 로깅, OpenAI SDK 연동, 테스트 자동화까지 가장 짧은 경로이기 때문.

- 판단해야 하는 것: Ollama 서빙과 optimizer 호출을 어떤 클라이언트로 통일할지.
- 고려한 선택지: Ollama 전용 클라이언트와 OpenAI SDK를 분리 / OpenAI SDK 하나로 통일.
- 판단: OpenAI SDK 하나로 통일.
- 이유: Ollama의 OpenAI-compatible endpoint를 그대로 사용할 수 있어 설정과 호출 표면적을 줄일 수 있기 때문.

- 판단해야 하는 것: Arize를 필수 경로로 둘지 여부.
- 고려한 선택지: 필수 의존성으로 고정 / 선택적 로깅 통합으로 처리.
- 판단: 선택적 로깅 통합으로 처리.
- 이유: PRD에서 Arize를 필수 의존성으로 두지 않았고, 핵심 실험 루프가 외부 SaaS 상태에 종속되면 전체 자동화가 약해지기 때문.

- 판단해야 하는 것: KORIE 입력 포맷이 저장소에 아직 없는 상태에서 데이터 계약을 어떻게 고정할지.
- 고려한 선택지: CSV / 폴더 스캔 기반 / JSONL manifest.
- 판단: JSONL manifest를 표준 입력 계약으로 고정.
- 이유: 샘플별 메타데이터, split, reference text를 같이 담기 쉽고 실험 로그와도 대칭 구조를 만들 수 있기 때문.

- 판단해야 하는 것: KORIE가 실제로 공개 접근 가능한지 여부.
- 고려한 선택지: 논문 기반 추정만 유지 / 공식 GitHub 저장소 확인 후 사용.
- 판단: 공식 GitHub 저장소 공개 확인 후 즉시 로컬로 반입.
- 이유: 사용자 요청이 다운로드 및 실사용까지 포함하고 있고, 공개 접근이 확인되면 더 이상 대기할 이유가 없기 때문.

- 판단해야 하는 것: KORIE 저장소만 사용할지, README의 외부 OCR split 아카이브까지 포함할지 여부.
- 고려한 선택지: 저장소 샘플만 사용 / README에 명시된 OCR split 전체를 내려받아 사용.
- 판단: OCR split 전체를 내려받아 사용.
- 이유: 샘플 2장으로는 PRD의 개발셋과 검증셋 절차를 충족할 수 없고, 사용자도 실제로 다운로드해 이용하라고 명시했기 때문.

- 판단해야 하는 것: 다운로드된 KORIE OCR split가 PRD 가정처럼 전체 영수증 OCR용인지 여부.
- 확인 결과: 공개 OCR split는 전체 영수증이 아니라 필드/단어 crop 이미지와 txt 정답으로 구성됨.
- 고려한 선택지: 공개 데이터 사용을 보류 / 현재 공개 OCR split를 우선 연결하고, 전체 영수증 실험은 별도 데이터 확보 전까지 후속으로 둠.
- 판단: 공개 OCR split를 우선 연결.
- 이유: 사용자 지시가 즉시 다운로드 및 활용이었고, 현재 공개 접근 가능한 정답 라벨 OCR 데이터는 이 경로이기 때문. 이 판단은 나중에 사용자가 전체 영수증 데이터로 교체 여부를 결정할 수 있도록 기록함.

- 판단해야 하는 것: Ollama OpenAI-compatible 비전 입력을 로컬 파일 URI로 줄지, data URL로 줄지 여부.
- 고려한 선택지: `file://` URI / base64 data URL.
- 판단: base64 data URL.
- 이유: Ollama OpenAI-compatible 구현에서 로컬 파일 URI 지원이 불안정할 수 있고, data URL이 더 이식성이 높기 때문.

- 확인 기록: `data/manifests/korie-ocr/one.jsonl` 1건으로 seed smoke run을 실행함.
- 결과: `P0` 기준 CER 0.0000으로 경로가 정상 동작함.
- 의미: 현재 코드 기준으로 `manifest -> Ollama GLM-OCR -> 평가/로그 저장` 흐름이 실제 실행 가능함을 확인.

- 실행 계획: 전체 실행 전, 5개 dev / 5개 val 샘플로 optimizer 경로를 smoke validation 한다.
- 이유: OpenAI optimizer 응답 형식과 Ollama OCR 호출을 함께 검증해야 전체 라운드 실패 비용을 줄일 수 있기 때문.

- 실행 계획: smoke run 성공 후 KORIE OCR 공개 manifest 전체(dev 60 / val 100)로 run-all을 실행한다.
- 주의: 이 데이터는 field crop OCR 벤치마크이며, 전체 영수증 OCR은 아님. 현재 공개 데이터 기준 최적화 결과로 기록한다.

- 구현 보완: validation 결과를 본 뒤 PRD의 최종 채택 규칙이 코드에 반영되지 않은 것을 확인.
- 조치: baseline/final validation 집계를 기반으로 adopted prompt를 결정하는 로직과 산출물(adopted_prompt.txt, report 반영)을 추가함.
- 이유: 최적화가 validation에서 실패한 경우 baseline을 채택해야 PRD와 일치하기 때문.

- 새로운 데이터셋 판단: 전체 영수증 OCR용 공개 데이터셋 후보로 CORD를 선택해 확인한다.
- 이유: 전체 receipt image가 있고 공개 접근 가능하며, 현재 KORIE crop split보다 PRD 가정에 더 가깝기 때문.

- CORD 실험 판단: train split rows API가 300MB 제한으로 실패하므로, 우선 first-rows로 안정적으로 가져올 수 있는 train 26 / val 100 구성으로 full-receipt 실험을 수행한다.
- 이유: 사용자 요청은 새 데이터셋으로 동일 실험을 계속 진행하는 것이고, 현재 가장 빠르게 end-to-end 검증 가능한 전체 영수증 경로이기 때문.

- CORD full-run 판단 변경: train 26 / val 100 full receipt run이 추론 시간상 과도하게 길어져 turn 내 완료 가능성이 낮음.
- 조치: full receipt 조건은 유지하되, 실행 가능성을 위해 더 작은 샘플 수로 동일 루프를 완료해 결과를 확보한다.

- full receipt 병목 대응: OCR client에서 이미지를 최대 1600px로 리사이즈 후 JPEG 인코딩하도록 변경.
- 이유: 원본 full receipt 이미지를 그대로 보내면 전송량과 추론 지연이 커져 실험 완료 시간이 과도하게 길어질 수 있기 때문.

- 한국어 공개 OCR 평가셋 판단: 영수증에 한정하지 않고 `한국어 이미지 + 정답 텍스트`가 붙은 공개 데이터셋을 다시 탐색함.
- 확인 결과: `HumynLabs/Korean_Receipts_Dataset`은 한국어 영수증 이미지 20장 공개지만 텍스트 라벨이 보이지 않아 OCR 평가셋으로는 직접 사용 불가. 반면 `AbdullahRian/Korean.OCR.Img.text.pair`는 이미지와 텍스트가 함께 공개되어 바로 OCR 평가용 manifest 생성 가능.
- 고려한 선택지: 영수증 이미지만 우선 수집 / 평가 가능한 텍스트 페어셋 우선 수집 / 둘 다 분리 수집.
- 판단: 둘 다 분리 수집.
- 이유: 사용자는 한국어 이미지면 충분하다고 했고, 동시에 OCR 평가에도 쓰길 원했다. 그래서 `이미지 전용 탐색셋`과 `이미지+텍스트 평가셋`을 각각 모으는 편이 목적에 가장 잘 맞는다.

- Arize 기록 누락 원인 조사: 기존 구현은 `https://app.arize.com/api/v1/space/log`로 집계 JSON을 직접 POST하고 있었음.
- 판단: `Arize AX` 기준 공식 엔드포인트인 `https://api.arize.com/v1/log`와 `Authorization`, `Grpc-Metadata-space_id` 헤더 형식으로 교체함.
- 이유: 사용자 확인 결과 사용 제품은 Phoenix가 아니라 Arize AX였고, 기존 구현은 실패를 삼켜 실제로는 아무 기록도 남지 않는 상태였음.

- KORIE full receipt 재구성 가능성 재검토: 공개 detection split를 실제로 다운로드해 내부 포맷을 확인함.
- 확인 결과: `labels/*.txt`는 클래스 id와 bbox 좌표만 포함하며, 박스별 transcription은 포함하지 않음.
- 판단: 현재 공개 detection split만으로는 full-page OCR reference text를 재구성하지 않음.
- 이유: 좌표 정보만으로는 plain text GT를 만들 수 없고, 이는 사용자가 허용한 느슨한 공백/줄바꿈 평가와는 다른 문제이기 때문. 텍스트 전사 자체가 없음.

- 다른 Codex가 내려받은 `korean-ocr-img-text-pair` split를 현재 loader에 연결하는 과정에서 경로 규칙 충돌이 발생함.
- 확인 결과: `prepare-split` 결과 manifest 일부가 manifest-relative 경로가 아니라 repo-root-relative 경로를 포함하고 있었고, 기존 `load_manifest`는 이를 무조건 manifest 디렉터리에 붙여 잘못된 경로를 만들었음.
- 판단: `load_manifest`에서 `manifest-relative 경로가 존재하면 우선 사용, 아니면 원래 상대경로를 그대로 사용`하는 보정 로직을 추가함.
- 이유: 기존 KORIE/기타 manifest와의 호환성을 깨지 않으면서, 이미 생성된 split manifest도 다시 쓸 수 있게 하는 편이 더 실용적이기 때문.

- `AbdullahRian/Korean.OCR.Img.text.pair` 실험 규모 판단: 60/40 full split로 `run-all`을 시도했으나 샘플 1건 OCR에 약 50초가 걸려 현실적으로 너무 느렸음.
- 고려한 선택지: full split 강행 / OCR client 추가 축소 / mini benchmark 별도 생성.
- 판단: 가장 짧은 line 이미지 위주로 `dev 2 / val 2` mini benchmark를 별도 생성해 경로만 먼저 검증함.
- 결과: seed 최적 prompt는 `P1`, 1라운드 2후보 최적화 후 mini validation에서 baseline과 optimized가 동률(CER 0.3409)로 나옴.

## 2026-03-14

### 판단 검토

1. 사용자 대신 내린 선택: 저장소가 비어 있는 상태에서 `Python + uv + CLI`로 전체 구현을 시작함.
- 상황: 구현 언어와 패키지 관리 방식을 처음 정해야 했음.
- 당시 대안: Python / Node.js, uv / pip 계열.
- 내가 그렇게 진행한 이유: 로컬 OCR 실험, OpenAI SDK, JSONL/CSV 처리, 테스트 자동화까지 가장 짧게 연결된다고 봤음.
- 사용자 검토용: 이 선택이 당신이 직접 했을 선택과 맞는지 확인 필요.

2. 사용자 대신 내린 선택: Ollama OCR 호출과 OpenAI optimizer 호출을 `OpenAI SDK` 하나로 통일함.
- 상황: 로컬 OCR과 원격 optimizer를 다른 클라이언트로 나눌지 정해야 했음.
- 당시 대안: 전용 Ollama client + OpenAI client 분리 / OpenAI-compatible 경로 하나로 통일.
- 내가 그렇게 진행한 이유: 설정과 오류 표면적을 줄이고 싶었음.
- 사용자 검토용: 당신도 “호환 경로 하나로 단순화”를 선호하는지 확인 필요.

3. 사용자 대신 내린 선택: Arize를 필수 경로가 아니라 선택적 통합으로 둠.
- 상황: PRD 핵심 실험 루프를 외부 SaaS에 종속시킬지 정해야 했음.
- 당시 대안: Arize 없으면 실행 불가 / Arize 없거나 깨져도 실험은 계속 진행.
- 내가 그렇게 진행한 이유: 전체 자동화가 멈추는 것을 피하려 했음.
- 사용자 검토용: 당신 의도는 “무조건 Arize에 남겨야 한다”에 가까운지, 아니면 “남으면 좋지만 실험이 우선”인지 확인 필요.

4. 사용자 대신 내린 선택: 데이터 표준 입력 계약을 `JSONL manifest`로 고정함.
- 상황: 데이터셋이 여러 종류로 늘어날 것을 예상했고, 공통 입력 포맷이 필요했음.
- 당시 대안: CSV / 폴더 스캔 / JSONL manifest.
- 내가 그렇게 진행한 이유: image path, reference text, split, metadata를 같이 담기 쉬웠음.
- 사용자 검토용: 이 정도의 명시적 데이터 계약이 당신 스타일과 맞는지 확인 필요.

5. 사용자 대신 내린 선택: KORIE 공개 여부를 확인한 뒤, 바로 로컬에 반입해서 사용함.
- 상황: 사용자가 “오픈소스면 다운로드해서 이용하라”고 했음.
- 당시 대안: 논문/README만 읽고 보류 / 즉시 다운로드 및 연결.
- 내가 그렇게 진행한 이유: 사용자의 지시가 명확했고, 공개 접근도 확인됐기 때문.
- 사용자 검토용: 이 정도로 바로 내려받아 붙이는 속도가 당신 기대와 맞는지 확인 필요.

6. 사용자 대신 내린 선택: KORIE 저장소 샘플이 아니라 README에 있는 OCR split 전체를 내려받아 사용함.
- 상황: 저장소 샘플만으로는 dev/val 실험을 만들기 어려웠음.
- 당시 대안: 샘플만 쓰기 / 외부 OCR split 전체 사용.
- 내가 그렇게 진행한 이유: PRD 절차를 실제로 굴리려면 샘플 몇 장으로는 부족하다고 봤음.
- 사용자 검토용: 당신도 “샘플보다 전체 split 우선”을 선택했을지 확인 필요.

7. 사용자 대신 내린 선택: KORIE 공개 OCR split가 full receipt는 아니지만, 우선 지금 공개된 OCR 정답셋으로 실험을 시작함.
- 상황: full receipt OCR GT가 바로 없었고, 당장 공개로 쓸 수 있는 것은 crop OCR이었음.
- 당시 대안: full receipt 공개셋 확보 전까지 보류 / crop OCR로 먼저 진행.
- 내가 그렇게 진행한 이유: 사용자는 “묻지 말고 끝까지 실행”을 원했고, 공개 정답셋으로 즉시 진행하는 편이 낫다고 봤음.
- 사용자 검토용: 이건 당신이 실제로도 허용했을 선택인지 확인 필요.

8. 사용자 대신 내린 선택: Ollama 비전 입력을 `base64 data URL`로 보냄.
- 상황: 로컬 파일 이미지를 OpenAI-compatible 메시지 포맷에 어떻게 실을지 정해야 했음.
- 당시 대안: `file://` URI / data URL.
- 내가 그렇게 진행한 이유: 구현 이식성과 호환성을 우선했음.
- 사용자 검토용: 이건 기술 디테일이지만, 당신은 단순함보다 성능 최적화를 더 우선했을 수도 있음.

9. 사용자 대신 내린 선택: `1건 smoke run`으로 기본 파이프라인 경로를 먼저 확인함.
- 상황: 빈 저장소에서 구현 직후 전체 실험을 바로 돌리기엔 리스크가 있었음.
- 당시 대안: 바로 full run / 1건 smoke run 후 확대.
- 내가 그렇게 진행한 이유: OCR 호출, 평가, 로그 저장이 실제로 붙는지 먼저 보고 싶었음.
- 사용자 검토용: 당신도 “우선 1건 확인 후 확대” 방식을 선호하는지 확인 필요.

10. 사용자 대신 내린 선택: 전체 실행 전 `5개 dev / 5개 val` smoke validation을 한 번 더 넣음.
- 상황: optimizer까지 포함한 경로를 전체 dev/val 전에 검증해야 했음.
- 당시 대안: 바로 full run / 소규모 smoke 후 full run.
- 내가 그렇게 진행한 이유: OpenAI optimizer 형식 오류나 OCR 호출 비용 문제를 미리 잡고 싶었음.
- 사용자 검토용: 당신 입장에선 이 중간 단계가 과하다고 볼 수도 있음.

11. 사용자 대신 내린 선택: smoke 뒤에는 바로 KORIE 공개 manifest 전체(dev 60 / val 100)로 `run-all`을 실행함.
- 상황: 사용자는 중간중간 개입 없이 끝까지 가라고 했음.
- 당시 대안: 더 오래 탐색 / 바로 본실험 진입.
- 내가 그렇게 진행한 이유: 일단 결과를 확보하는 것이 우선이라고 봤음.
- 사용자 검토용: 당신도 이 타이밍에 “일단 전체 실험 결과 확보”를 원했는지 확인 필요.

12. 사용자 대신 내린 선택: validation 결과를 본 뒤 PRD 채택 규칙을 코드에 보완함.
- 상황: 초기 구현에서 adopted prompt 규칙이 빠져 있었음.
- 당시 대안: 바로 수정 / 일단 결과 보고 나중에 수정.
- 내가 그렇게 진행한 이유: 처음엔 루프 구현을 우선했고, validation에서 문제를 확인한 뒤 보완했음.
- 사용자 검토용: 이건 당신이라면 “처음부터 넣었어야 한다”고 볼 가능성이 큼.

13. 사용자 대신 내린 선택: 새 데이터셋 후보로 `CORD`를 먼저 선택함.
- 상황: 사용자가 “전체 영수증을 통째로 OCR하는 데이터셋이면 다를 수 있다”고 했음.
- 당시 대안: 더 오래 한국어 full receipt를 찾기 / 공개 full receipt인 CORD로 먼저 검증.
- 내가 그렇게 진행한 이유: full receipt 가설을 빠르게 검증할 수 있는 공개셋이 CORD였음.
- 사용자 검토용: 당신은 “한국어가 아니면 의미가 약하다”고 봤을 수도 있음.

14. 사용자 대신 내린 선택: CORD를 처음엔 `train 26 / val 100` 수준으로 시도함.
- 상황: HF API 제한이 있었고, full receipt 실험은 하고 싶었음.
- 당시 대안: 더 작은 셋 / 가능한 최대한 크게 시도.
- 내가 그렇게 진행한 이유: directionality를 보기엔 sample이 너무 작지 않길 바랐음.
- 사용자 검토용: 당신 기준에는 애초에 더 작은 셋으로 빠르게 돌리는 편이 맞았을 수 있음.

15. 사용자 대신 내린 선택: CORD full run이 너무 길어지자 subset으로 축소해서 끝까지 완료함.
- 상황: full receipt OCR이 turn 안에 완료되지 않았음.
- 당시 대안: 끝까지 기다리기 / subset으로 축소해 실험 완료.
- 내가 그렇게 진행한 이유: “부분 구현”이 아니라 결과 확보를 우선했음.
- 사용자 검토용: 이건 당신도 아마 허용했을 가능성이 높지만, 그래도 full-size 우선주의였는지 확인 필요.

16. 사용자 대신 내린 선택: full receipt 병목 완화를 위해 OCR client에 `max_dimension=1600` 리사이즈를 넣음.
- 상황: 원본 full receipt 이미지가 너무 무거웠음.
- 당시 대안: 전처리 없이 유지 / 리사이즈 추가.
- 내가 그렇게 진행한 이유: 전송량과 추론 시간을 줄이는 가장 빠른 조치였음.
- 사용자 검토용: 당신은 “정확도 리스크가 있어도 속도 우선”인지, “원본 보존 우선”인지 확인 필요.

17. 사용자 대신 내린 선택: 한국어 공개 OCR 평가셋을 영수증에 한정하지 않고 다시 탐색함.
- 상황: 사용자가 “꼭 영수증이 아니어도 된다”고 명시함.
- 당시 대안: 계속 영수증만 찾기 / 범용 한국어 OCR까지 확대.
- 내가 그렇게 진행한 이유: 공개 접근 가능한 평가셋을 더 빨리 확보할 수 있었음.
- 사용자 검토용: 이건 아마 당신 의도와 맞을 가능성이 높음.

18. 사용자 대신 내린 선택: `이미지 전용 탐색셋`과 `이미지+텍스트 평가셋`을 분리 수집함.
- 상황: 공개셋마다 라벨 가용성이 달랐음.
- 당시 대안: 평가셋만 수집 / 이미지셋만 수집 / 둘 다 분리 수집.
- 내가 그렇게 진행한 이유: 사용자가 나중에 수동 라벨링이나 샘플 확인을 할 여지도 남기고 싶었음.
- 사용자 검토용: 당신도 이런 이원화 수집을 원했는지 확인 필요.

19. 사용자 대신 내린 선택: Arize AX 여부를 확인한 뒤에야 로거를 고침.
- 상황: `.env`에는 ARIZE 값이 있었지만 제품 타입은 확인 전이었음.
- 당시 대안: Phoenix 기준으로 가정 / 제품 확인 후 수정.
- 내가 그렇게 진행한 이유: 잘못된 제품 기준으로 다시 엇나갈 수 있었기 때문.
- 사용자 검토용: 이건 “한 번 물어본 것”이 맞는 상황이었는지 확인 필요.

20. 사용자 대신 내린 선택: KORIE detection split로 full-page OCR GT를 만들 수 있을지 실제 다운로드해 확인함.
- 상황: 사용자가 “박스 위치가 있으면 가능할 것 같다”고 했음.
- 당시 대안: 가정만 유지 / 실제 zip 내부 포맷 확인.
- 내가 그렇게 진행한 이유: 재구성 가능성은 추측으로 둘 수 없다고 봤음.
- 사용자 검토용: 당신도 이 정도는 확인 후 판단하는 흐름을 원했는지 확인 필요.

21. 사용자 대신 내린 선택: 다른 Codex가 만든 split manifest를 loader 보정으로 호환 처리함.
- 상황: repo-relative 경로가 들어 있어 실험이 바로 깨졌음.
- 당시 대안: manifest 재생성 / loader를 보정해 둘 다 허용.
- 내가 그렇게 진행한 이유: 기존 산출물을 버리지 않고, 호환 범위를 넓히는 편이 낫다고 봤음.
- 사용자 검토용: 당신은 이런 유연성보다 입력 포맷 엄격화를 더 선호할 수도 있음.

22. 사용자 대신 내린 선택: `AbdullahRian/Korean.OCR.Img.text.pair` full split가 너무 무거워 `mini benchmark`를 먼저 만듦.
- 상황: line 이미지 일부가 초장폭이라 전체 루프 시간이 과도했음.
- 당시 대안: full split 강행 / 전처리 먼저 수정 / mini benchmark로 경로만 확인.
- 내가 그렇게 진행한 이유: 공개 한국어 OCR 평가셋을 실제로 파이프라인에 연결했다는 증거를 먼저 남기고 싶었음.
- 사용자 검토용: 당신은 아마 “전처리부터 고치고 full split”를 더 원했을 수도 있음.

23. 사용자 대신 내린 선택: mini benchmark 결과를 별도 문서로 남김.
- 상황: full split는 아직 못 돌렸지만, mini 실험 결과는 있었음.
- 당시 대안: 문서화 없이 넘어감 / 작은 결과라도 남김.
- 내가 그렇게 진행한 이유: 다음 실험에서 중복 설명 비용을 줄이고 싶었음.
- 사용자 검토용: 당신은 작은 결과 문서화보다 바로 다음 구현을 더 선호할 수도 있음.

24. 사용자 대신 내린 선택: 다른 Codex가 만든 `docs/003`, `docs/004`를 보존하고 커밋에 포함함.
- 상황: 사용자 요청상 다른 Codex 작업도 읽고 필요한 걸 이어가야 했음.
- 당시 대안: 참고만 하고 버림 / 문서도 저장소 자산으로 편입.
- 내가 그렇게 진행한 이유: 데이터셋 전략 문서로 재사용 가치가 있다고 봤음.
- 사용자 검토용: 당신은 실험에 직접 쓰이는 파일만 커밋하길 원했을 수도 있음.

### 사용자 리뷰 반영

- 위 선택들은 전반적으로 사용자 의도와 맞는 것으로 확인됨.
- 단, `dimension` 축소처럼 정확도에 영향을 줄 수 있는 전처리는 적용 후 반드시 품질 문제가 없는지 확인해야 함.
- 앞으로 `codex-log.md`에는 자잘한 구현 선택은 기록하지 않고, 사용자가 직접 판단했을 가능성이 높은 선택만 기록함.
